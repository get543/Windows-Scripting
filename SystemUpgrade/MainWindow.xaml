<Window x:Name="window" x:Class="SystemUpgradeGUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SystemUpgradeGUI"
        mc:Ignorable="d"
        Title="System Upgrade GUI" Height="740" Width="760"
        WindowStartupLocation="CenterScreen"
        Background="#1C1C1C" Foreground="#FFFFFF"
        FontFamily="Segoe UI Variable Text, Segoe UI, sans-serif">

    <Window.Resources>
        <!-- Modern Card Style -->
        <Style x:Key="CardBorder" TargetType="Border">
            <Setter Property="Background" Value="#272727"/>
            <Setter Property="BorderBrush" Value="#383838"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20,16"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
        </Style>

        <!-- Modern Section Header Style -->
        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#0078D4"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <!-- Modern CheckBox Style -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#EEEEEE"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,6,0,6"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid Background="Transparent">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border x:Name="chkBorder" Width="18" Height="18" CornerRadius="4" 
                                    Background="#2D2D2D" BorderBrush="#555555" BorderThickness="1"
                                    VerticalAlignment="Center">
                                <Path x:Name="chkPath" Data="M 3,8 L 7,12 L 14,4" 
                                      Stroke="#FFFFFF" StrokeThickness="2" 
                                      Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ContentPresenter Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="chkBorder" Property="Background" Value="#0078D4"/>
                                <Setter TargetName="chkBorder" Property="BorderBrush" Value="#0078D4"/>
                                <Setter TargetName="chkPath" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="chkBorder" Property="BorderBrush" Value="#0078D4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern Primary Button Style -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="btnBorder" Background="{TemplateBinding Background}" 
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="btnBorder" Property="Background" Value="#106EBE"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="btnBorder" Property="Background" Value="#005A9E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header Title Section -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <Label x:Name="ScriptsTitle" Content="System Upgrade Manager" 
                   FontSize="24" FontWeight="Bold" Foreground="#FFFFFF" Padding="0" Margin="0,0,0,4"/>
            <TextBlock Text="Select system maintenance and package updates to run." 
                       FontSize="13" Foreground="#999999"/>
        </StackPanel>

        <!-- Main Content Area with Cards -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Focusable="False" Margin="0,0,0,10">
            <StackPanel>
                <!-- Card 1: Package & App Upgrades -->
                <Border Style="{StaticResource CardBorder}">
                    <StackPanel>
                        <TextBlock Text="Package &amp; Application Updates" Style="{StaticResource SectionHeader}"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <CheckBox x:Name="UpdatePSModule" Content="Update All PowerShell Modules"/>
                                <CheckBox x:Name="WindowsUpdate" Content="Update All Windows Update"/>
                                <CheckBox x:Name="MicrosoftStoreUpdate" Content="Update All Microsoft Store Applications"/>
                                <CheckBox x:Name="WingetUpgrade" Content="Update All Winget Package Manager Applications"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1">
                                <CheckBox x:Name="ChocoUpgrade" Content="Update All Chocolatey Package Manager"/>
                                <CheckBox x:Name="NPMUpgrade" Content="Update All NPM Global Packages"/>
                                <CheckBox x:Name="PipUpgrade" Content="Update All Python Pip Packages"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Card 2: System Maintenance -->
                <Border Style="{StaticResource CardBorder}">
                    <StackPanel>
                        <TextBlock Text="System Maintenance" Style="{StaticResource SectionHeader}"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <CheckBox x:Name="CheckCorruptionFiles" Content="Scan &amp; Repair Corruption (sfc / DISM / chkdsk)"/>
                            </StackPanel>

                            <StackPanel Grid.Column="1">
                                <CheckBox x:Name="DeleteTempFiles" Content="Delete Temporary Files (Disk Cleanup &amp; Temp Files)"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Status & Progress Section -->
        <Grid Grid.Row="2" Margin="0,0,0,16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Console Output" FontSize="13" FontWeight="SemiBold" Foreground="#0078D4" Margin="0,0,0,6"/>
            <Border Grid.Row="1" Background="#111111" BorderBrush="#383838" BorderThickness="1" CornerRadius="6" MinHeight="140" Margin="0,0,0,12" Padding="8">
                <TextBox x:Name="OutputTextBox" Background="Transparent" Foreground="#D4D4D4" BorderThickness="0" 
                         FontFamily="Consolas, Courier New, monospace" FontSize="12" AcceptsReturn="True" 
                         TextWrapping="Wrap" IsReadOnly="True" VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Disabled" VerticalAlignment="Stretch"/>
            </Border>

            <Label Grid.Row="2" x:Name="RunningTask" Content="Ready" 
                   Foreground="#CCCCCC" FontSize="13" FontWeight="Medium" Padding="0,0,0,6" HorizontalAlignment="Left"/>
            <Border Grid.Row="3" Background="#2D2D2D" CornerRadius="3" Height="6" ClipToBounds="True">
                <ProgressBar x:Name="ProgressBar" Height="6" Background="Transparent" Foreground="#0078D4" 
                             BorderThickness="0" IsIndeterminate="False" Minimum="0" Maximum="100" Value="0"/>
            </Border>
        </Grid>

        <!-- Action Bar -->
        <Grid Grid.Row="3">
            <Button x:Name="RunButton" Style="{StaticResource PrimaryButtonStyle}" 
                    Content="Run Selected Tweaks" Width="180" Height="38" Padding="12,0" HorizontalAlignment="Right"/>
        </Grid>
    </Grid>
</Window>
